import { FilterLinksByGroupPipe } from './filter-links-by-group.pipe';

describe('FilterLinksByGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLinksByGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
