import { NumberToWordMonthPipe } from './number-to-word-month.pipe';

describe('NumberToWordMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToWordMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
