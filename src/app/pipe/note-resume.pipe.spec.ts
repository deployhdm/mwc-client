import { NoteResumePipe } from './note-resume.pipe';

describe('NoteResumePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteResumePipe();
    expect(pipe).toBeTruthy();
  });
});
