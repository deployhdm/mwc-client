import { WordToLetterDayOfWeekPipe } from './word-to-letter-day-of-week.pipe';

describe('WordToLetterDayOfWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new WordToLetterDayOfWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
