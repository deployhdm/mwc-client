import { DatePlanningPipe } from './date-planning.pipe';

describe('DatePlanningPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePlanningPipe();
    expect(pipe).toBeTruthy();
  });
});
